# X Fetcher Skill 项目总结

## 项目概述

本项目是一个封装的 Skill，基于 [Jane-xiaoer/x-fetcher](https://github.com/Jane-xiaoer/x-fetcher) 项目，用于抓取 X (Twitter) 推文和长文章，并自动保存为 Markdown 格式。

## 项目结构

```
x-fetcher-skill/
├── SKILL.md                  # Skill 主文档（Agent 使用）
├── README.md                 # 项目说明文档
├── USAGE.md                  # 详细使用指南
├── EXTEND.md.example         # 配置文件示例
├── scripts/
│   ├── main.py              # 主脚本（增强版）
│   └── requirements.txt     # Python 依赖
├── fetch_x.py               # 原始抓取脚本
├── requirements.txt         # 原始依赖
└── LICENSE                  # MIT License
```

## 核心功能

### 1. 抓取推文和文章
- ✅ 普通推文（文字、图片、视频链接）
- ✅ X Article 长文章（完整正文，Markdown 格式）
- ✅ 互动数据（点赞、转发、浏览量、书签数）

### 2. 自动保存
- ✅ 默认自动保存为 Markdown 文件
- ✅ 支持配置默认下载目录
- ✅ 文件命名: `{username}/{tweet-id}.md`

### 3. 配置支持
- ✅ 支持配置文件（EXTEND.md）
- ✅ 项目级和用户级配置
- ✅ 首次运行引导配置

### 4. 灵活输出
- ✅ JSON 格式输出
- ✅ Markdown 格式输出
- ✅ 自定义输出路径

## 技术实现

### 核心脚本 (scripts/main.py)

增强版主脚本，添加了以下功能：
1. 配置文件加载和解析
2. 首次运行检测和引导
3. 自动目录创建
4. 更灵活的命令行参数

### 配置系统

配置优先级：
1. 命令行参数
2. 项目级配置 (`.x-fetcher/EXTEND.md`)
3. 用户级配置 (`~/.x-fetcher/EXTEND.md`)
4. 默认值

配置格式（YAML）：
```yaml
---
default_output_dir: ~/Documents/x-posts
auto_save: true
download_media: ask
---
```

### 数据流

```
URL 输入
  ↓
配置检查
  ↓
调用 fetch_x.py 抓取数据
  ↓
格式化为 Markdown
  ↓
保存到配置的目录
  ↓
输出结果（JSON + Markdown 路径）
```

## 使用场景

### 场景 1: 个人知识管理
```
用户发现优质推文 → 使用 x-fetcher 保存 → 自动归档到知识库
```

### 场景 2: 内容研究
```
研究人员收集推文 → 批量抓取 → 分析互动数据
```

### 场景 3: 内容创作
```
创作者保存灵感推文 → 整理成 Markdown → 用于创作参考
```

### 场景 4: Claude Code 集成
```
用户向 Claude 提供推文 URL → Claude 自动调用 Skill → 保存并整理内容
```

## 与原项目的区别

| 特性 | 原项目 | 本 Skill |
|------|--------|----------|
| 基本抓取功能 | ✅ | ✅ |
| 配置文件支持 | ❌ | ✅ |
| 自动保存 | 需要参数 | 默认自动 |
| 首次运行引导 | ❌ | ✅ |
| Skill 集成 | ❌ | ✅ |
| 目录自动创建 | ❌ | ✅ |
| 详细文档 | 基础 | 完整 |

## 限制和注意事项

### 技术限制
- ⚠️ 依赖第三方 API（fxtwitter）
- ⚠️ 私密账号内容无法抓取
- ⚠️ 部分媒体 URL 可能不完整

### 使用建议
- 📌 首次使用前配置默认下载目录
- 📌 定期备份抓取的内容
- 📌 遵守 Twitter 使用条款
- 📌 不要过于频繁地请求

## 安装和部署

### 本地安装

```bash
# 1. 克隆项目
cd /Users/m/document/QNSZ/project/skills
git clone https://github.com/Jane-xiaoer/x-fetcher.git x-fetcher-skill

# 2. 安装依赖
cd x-fetcher-skill
pip3 install -r scripts/requirements.txt

# 3. 创建配置
mkdir -p ~/.x-fetcher
cat > ~/.x-fetcher/EXTEND.md << 'EOF'
---
default_output_dir: ~/Documents/x-posts
---
EOF

# 4. 测试
python3 scripts/main.py --check-config
```

### 作为 Skill 使用

```bash
# 链接到 Claude Code
ln -s /Users/m/document/QNSZ/project/skills/x-fetcher-skill ~/.claude/skills/x-fetcher
```

## 测试

### 测试配置

```bash
python3 scripts/main.py --check-config
```

### 测试抓取（普通推文）

```bash
python3 scripts/main.py "https://x.com/elonmusk/status/123456789" --json
```

### 测试抓取（X Article）

```bash
python3 scripts/main.py "https://x.com/thedankoe/status/2010751592346030461"
```

## 未来改进方向

### 短期
- [ ] 添加媒体文件下载功能
- [ ] 支持推文线程抓取
- [ ] 添加代理支持

### 中期
- [ ] Web 界面
- [ ] 数据库存储
- [ ] 全文搜索

### 长期
- [ ] 多平台支持（Mastodon, Threads）
- [ ] 数据分析和可视化
- [ ] API 服务

## 贡献

本项目基于 [Jane-xiaoer/x-fetcher](https://github.com/Jane-xiaoer/x-fetcher) 开发，欢迎贡献代码和建议。

## License

MIT License

## 联系方式

- 原项目: https://github.com/Jane-xiaoer/x-fetcher
- Skills 社区: https://skills.sh/

---

**最后更新**: 2026-02-18
**版本**: 1.0.0
**作者**: 基于 Jane-xiaoer/x-fetcher 项目封装
