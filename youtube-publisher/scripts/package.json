{
  "name": "youtube-publisher",
  "version": "1.5.0",
  "type": "commonjs",
  "description": "Upload and publish videos to YouTube with thumbnails and subtitles",
  "scripts": {
    "upload": "ts-node youtube-upload.ts",
    "typecheck:production": "tsc --noEmit --target ES2020 --module CommonJS --moduleResolution node --esModuleInterop --strict --skipLibCheck youtube-upload.ts list-uploads.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "googleapis": "^129.0.0",
    "open": "^8.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.33",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
